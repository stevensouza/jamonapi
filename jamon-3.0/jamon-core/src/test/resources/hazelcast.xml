<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xsi:schemaLocation="http://www.hazelcast.com/schema/config hazelcast-config-5.5.xsd"
           xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <cluster-name>test</cluster-name>
    
    <properties>
        <!-- Disable all discovery mechanisms for faster startup -->
        <property name="hazelcast.discovery.enabled">false</property>
        <property name="hazelcast.shutdownhook.enabled">false</property>
        <property name="hazelcast.wait.seconds.before.join">0</property>
        <property name="hazelcast.max.wait.seconds.before.join">1</property>
        <!-- Aggressive partition optimization -->
        <property name="hazelcast.partition.count">1</property>
        <property name="hazelcast.partition.migration.timeout">1</property>
        <property name="hazelcast.partition.table.send.interval">1</property>
        <property name="hazelcast.partition.migration.interval">1</property>
        <property name="hazelcast.partition.backup.sync.timeout">1000</property>
        <!-- Fast operation timeouts -->
        <property name="hazelcast.operation.call.timeout.millis">3000</property>
        <property name="hazelcast.operation.backup.timeout.millis">1000</property>
        <!-- Disable health monitoring -->
        <property name="hazelcast.health.monitoring.level">OFF</property>
        <property name="hazelcast.health.monitoring.delay.seconds">86400</property>
        <!-- Disable multicast completely for test environment -->
        <property name="hazelcast.discovery.enabled">false</property>
        <property name="hazelcast.socket.bind.any">false</property>
        <!-- Force unisocket mode for testing -->
        <property name="hazelcast.unisocket.enabled">false</property>
        <!-- Disable phone home to prevent shutdown errors -->
        <property name="hazelcast.phone.home.enabled">false</property>
    </properties>
    
    <network>
        <port auto-increment="false">5701</port>
        <join>
            <!-- Explicitly use TCP-IP with localhost only -->
            <multicast enabled="false" />
            <tcp-ip enabled="true">
                <member>127.0.0.1:5701</member>
            </tcp-ip>
            <auto-detection enabled="false" />
        </join>
        <interfaces enabled="true">
            <interface>127.0.0.1</interface>
        </interfaces>
    </network>
    
    <!-- Minimal configuration for testing -->
    <map name="default">
        <backup-count>0</backup-count>
        <async-backup-count>0</async-backup-count>
    </map>
    
</hazelcast>